
module.exports={
    pi: 3.14,
    max: (a,b) => {
        if(a>b){
            return `${a}`;
        }
        else{
            return `${a}`;
        }
    },
    sqrt: (a) => {
        return Math.sqrt(a);
    },
    floor: (a)=> {
        return Math.floor(a);
    },
    abs: (a) => {
        return Math.abs(a);
    }
}